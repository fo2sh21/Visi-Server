# Rust build artifacts
/target/
**/*.rs.bk

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Local dev databases / caches
*.db
*.db-*
.pytest_cache/
.mypy_cache/

# Env / secrets
.env
*.pem
*.key

# Firebase / service credentials (never commit)
google-services.json
*-service-account*.json
service-account*.json
credentials*.json
FCM_CREDENTIALS_JSON.json
